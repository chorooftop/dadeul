---
type: action
id: action-region-resolve
description: "GPS 좌표 → 시군구 지역코드 변환. 판별을 클라이언트에 맡기면 기기별 지도 데이터 차이로 같은 위치가 다른 지역이 된다"
stage: 1
refs:
  - entity-region
  - term-region-code
used_by:
  - spec-weather-vote-widget
---

# 지역판별

**구현 앵커:**

| 레이어 | 경로 |
|------|------|
| route | `apps/api/src/routes/regions.ts` — `POST /v1/regions/resolve`, `GET /v1/regions` |
| service | `apps/api/src/services/regions.ts` — 카카오 coord2regioncode(법정동 B 코드 상위 5자리), 지역 lazy upsert |
| test | `apps/api/test/api.test.ts` — 판별·캐시·REGION_UNRESOLVED |

## Rationale

_(TBD)_

## Auth & Permissions

- 익명 기기 계정 토큰. 위치 권한 거부 유저의 대체 플로우(수동 지역 선택)는 spec에서 다룬다

## Execution Order

1. 클라이언트가 좌표(위도·경도)를 전송 — 좌표는 판별 즉시 폐기, 저장 금지
2. 서버가 [지역코드][term-region-code]로 변환 ([지역][entity-region] 마스터와 대조)
3. 계정의 캐시 지역 갱신 후 지역 정보 응답

## Input / Output

- 입력: 위도·경도
- 출력: 지역코드, 표시명, 서비스 활성 여부

## DO NOT

- 좌표 원본을 로그·DB에 저장하지 않는다 — 프라이버시 원칙: 시군구 코드만이 저장 최소 단위
- 클라이언트 로컬 지오코딩 결과를 신뢰하지 않는다

## Error Handling

_(TBD)_

| 조건 | 토큰 |
|---|---|
| 판별 불가 좌표(해외 등) | `REGION_UNRESOLVED` |
| 비활성 지역 | `REGION_INACTIVE` |

<!-- TOM refs -->
[entity-region]: ../entity/지역.md
[term-region-code]: ../term/지역코드.md
